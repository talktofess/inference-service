# Core service + benchmark harness. Deliberately NO torch/vllm here — those are
# GPU-only and live in requirements-gpu.txt, so the gateway, benchmark harness,
# cost model, and dashboards install and run on a CPU box.
pydantic>=2.7
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
prometheus-client>=0.20
matplotlib>=3.8
